function handleCategoryChange() {
    var selectedCategory = document.getElementById('categoryDropdown').value;
    // Lakukan sesuatu dengan nilai kategori yang dipilih, misalnya:
    console.log('Kategori dipilih:', selectedCategory);
}

function handleCategorySortChange() {
    var selectedSort = document.getElementById('sortDropdown').value;
    // Lakukan sesuatu dengan nilai kategori yang dipilih, misalnya:
    console.log('Sort dipilih:', selectedSort);
}